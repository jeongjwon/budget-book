# 📔 Budget Book (가계부 서비스)

개인 자산 관리의 편의성과 시각적 통찰력을 제공하는 웹 기반 가계부 서비스입니다. 수입과 지출을 직관적으로 기록하고, 다양한 시각화 도구를 통해 소비 습관을 파악할 수 있도록 돕습니다.

## 🛠 기술 스택

### Frontend

- **Framework:** Next.js 16 (App Router)
- **Library:** React 19, TypeScript
- **Styling:** Tailwind CSS 4
- **Server State:** TanStack Query
- **Client State:** Zustand
- **Visualization:** Recharts
- **Icons:** Lucide React

### Backend & Database

- **BaaS:** Supabase (PostgreSQL, Authentication)
- **Auth:** Social Login (Google, Kakao) — OAuth 2.0 PKCE Flow

---

## ✨ 주요 핵심 기능

### 1. 직관적인 대시보드 및 데이터 시각화

- **월별 수입/지출 흐름:** Recharts를 활용하여 연간 흐름을 막대 그래프로 시각화하며, 현재 선택된 월로 자동 스크롤되는 인터랙티브 요소를 포함합니다.
- **카테고리별 분포:** 파이 차트를 통해 지출과 수입의 비중을 한눈에 파악할 수 있습니다.
- **월별 요약:** 총 수입, 총 지출, 잔액을 상단에 고정하여 즉각적인 자산 상태 확인이 가능합니다.

### 2. 다각도 거래 내역 확인

- **캘린더 뷰:** 달력 형식으로 일별 지출 분포를 확인하고, 특정 날짜를 클릭하여 바로 거래를 추가할 수 있습니다.
- **리스트 뷰:** 최신순으로 정렬된 거래 내역을 상세하게 확인할 수 있습니다.

### 3. 자주 쓰는 거래 (템플릿 기능)

- 반복되는 지출이나 수입을 템플릿으로 등록하여, 한 번의 탭으로 금액과 카테고리가 입력된 모달을 띄워 빠른 기록을 지원합니다.
- 사용자별 맞춤 템플릿 관리 기능을 제공합니다.

### 4. 월별 리포트 내보내기

- 선택한 월의 가계부를 HTML 파일로 내보낼 수 있으며, 내보낸 파일 내에서 CSV 다운로드도 지원합니다.
- 리포트에는 수입/지출 요약 카드, 카테고리별 파이 차트(SVG), 일별 내역 테이블, 전체 거래 내역이 포함됩니다.
- 외부 라이브러리 없이 순수 HTML/SVG 생성 방식으로 구현하여 오프라인 환경에서도 열람 가능합니다.

### 5. 소셜 로그인 및 세션 관리

- Google, Kakao 소셜 로그인을 통해 별도의 가입 절차 없이 간편하게 이용 가능합니다.
- Row Level Security(RLS)를 적용하여 사용자의 데이터가 안전하게 격리되고 보호됩니다.
- OAuth 2.0 PKCE Flow 기반 로그인으로 토큰 탈취 방지, 자동 세션 갱신, 만료 시 자동 로그아웃을 지원합니다.

### 6. 모바일 퍼스트 반응형 디자인

- Tailwind CSS를 활용하여 모바일 환경에 최적화된 하단 탭 바와 플로팅 버튼 UI를 설계했습니다.
- 모든 모달과 입력 폼이 작은 화면에서도 조작하기 쉽도록 레이아웃을 구성했습니다.

---

## 🚀 이슈 해결 및 기술적 고민

### 1. 사용자 경험(UX) 최적화: 자동 스크롤 및 인터랙티브 차트

- **문제:** 12개월치 데이터를 보여주는 차트에서 현재 달의 데이터를 찾기 위해 사용자가 수동으로 스크롤해야 하는 번거로움이 있었습니다.
- **해결:** `useRef`와 `useEffect`를 활용하여 차트 컨테이너의 너비와 선택된 월의 위치를 계산, 페이지 로드 시 또는 월 변경 시 현재 달이 중앙에 오도록 자동 스크롤 기능을 구현했습니다.

### 2. 효율적인 상태 관리와 데이터 동기화

- **문제:** 다수의 컴포넌트(캘린더, 리스트, 차트, 모달)에서 동일한 거래 데이터를 공유하고 조작해야 하는 복잡성이 있었습니다.
- **해결:** Zustand를 사용하여 전역 상태를 관리하고, Supabase와의 실시간 통신 및 낙관적 업데이트(Optimistic Updates) 개념을 적용하여 데이터 CRUD 시 UI 반영 속도를 극대화했습니다.

### 3. 유연한 데이터 모델링: 자주 쓰는 거래 (Templates)

- **문제:** 초기 기획에서는 고정된 카테고리만 제공하려 했으나, 사용자마다 반복되는 지출 패턴이 다르다는 점에 착안했습니다.
- **해결:** 별도의 `transaction_templates` 테이블을 설계하고 이를 관리할 수 있는 `useTemplates` 커스텀 훅을 개발하여, 사용자가 직접 자신만의 퀵 메뉴를 생성하고 편집할 수 있는 유연한 기능을 제공했습니다.

### 4. PKCE 코드 이중 교환으로 인한 로그인 세션 실패 디버깅

- **문제:** OAuth 로그인 후 `/auth/callback`으로 리다이렉트될 때 세션이 생성되지 않고 `/login?error=callback_failed`로 튕기는 문제가 있었습니다.
- **원인 분석:** `@supabase/supabase-js` v2는 클라이언트 생성자에서 `initialize()`를 즉시 호출하며, `detectSessionInUrl: true`(기본값) 설정에 따라 URL에 `?code=` 파라미터가 감지되면 PKCE 코드 교환을 **자동으로** 수행합니다. 기존 콜백 페이지에서 `exchangeCodeForSession(code)`를 한 번 더 수동 호출하면, 이미 소비된 코드를 재사용하려는 시도가 되어 Supabase 서버가 `invalid_grant` 에러를 반환했습니다. 콜백 페이지가 이 에러를 감지하고 로그인 실패 페이지로 리다이렉트하는 것이 근본 원인이었습니다.
- **해결:** 수동 `exchangeCodeForSession` 호출을 제거하고, `supabase.auth.initialize()`를 명시적으로 `await`하여 자동 코드 교환 완료를 기다린 뒤 에러 유무를 확인하도록 수정했습니다. 또한 `onAuthStateChange`에서 `SIGNED_OUT` 이벤트(리프레시 토큰 만료 등 자동 로그아웃 포함)를 명시적으로 처리해 거래 데이터도 함께 초기화되도록 보완했습니다.

### 5. 외부 의존성 없는 리포트 내보내기 구현

- **문제:** 월별 데이터를 PDF나 엑셀로 내보내는 기능을 구현하기 위해 `jsPDF`, `xlsx` 같은 외부 라이브러리 도입을 검토했으나, 번들 사이즈 증가와 스타일 커스터마이징의 한계가 문제였습니다.
- **해결:** 순수 HTML 문자열과 SVG를 직접 생성하여 Blob으로 다운로드하는 방식을 채택했습니다. 카테고리별 파이 차트는 삼각함수로 좌표를 계산해 SVG `<path>`로 직접 그렸으며, 파일 내부에 거래 데이터를 JSON으로 임베드하여 열람 후 CSV로도 추가 내보내기가 가능하도록 구현했습니다.

---

## 📂 Other Projects

### 2. Budget Book (React, TypeScript, Next.js, Supabase, TanStack Query, Zustand)

**2026.05 | 개인 프로젝트**

수입·지출을 직접 기록하고 시각적으로 소비 패턴을 돌아볼 수 있는 가계부 웹 서비스 개발

- Google·Kakao OAuth 로그인, 거래 기록/통계 시각화(막대·파이 차트), 퀵 템플릿, 월별 리포트 내보내기 구현
- Supabase RLS로 DB 레벨 사용자 데이터 격리 설계 / TanStack Query로 서버 상태(거래 내역·템플릿) 캐싱·재검증을 위임하고 Zustand는 UI 상태(선택 월, 모달 등)만 담당하도록 역할 분리 / `jsPDF` 없이 순수 HTML·SVG를 직접 생성해 리포트 구현 — 번들 최소화, 오프라인 열람, 디자인 자유도를 동시에 확보
- Kakao 로그인 세션 미생성 버그를 환경변수·콜백 URL 추측으로 접근했다가 해결 못하고, Supabase 소스를 직접 열어 자동 PKCE 코드 교환 동작을 확인한 뒤 이중 호출 문제를 해결 — 추측이 아닌 소스 확인이 더 빠르다는 것을 체감 / 초기에 Zustand 단일 스토어로 서버·클라이언트 상태를 함께 관리하다 낙관적 업데이트의 롤백·재검증 처리가 점점 복잡해지는 한계를 겪은 뒤 TanStack Query로 서버 상태를 분리해 직접 리팩토링
